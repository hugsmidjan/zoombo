import o from 'ospec';
import { eventHandlers, actions } from './Zoombo.privates';
import Zooombo from './Zoombo';

o.spec('Zooombo eventHandlers', () => {
  o('...', () => {
    o(true).equals(true);
  });
});

o.spec('Zooombo actions', () => {
  o('...', () => {
    o(true).equals(true);
  });
});

o.spec('Zooombo instance', () => {
  o('...', () => {
    o(true).equals(true);
  });
});
